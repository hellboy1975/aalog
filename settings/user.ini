; You can play around with the settings in this file.  You can still break
; stuff though, so be careful (and read the comments!)

; You should really change this file through the user:config:set commands (not yet implemented)
[default]

; location of the aalog database file.  Default "aalog.db"
DatabaseFile = "aalog.db"